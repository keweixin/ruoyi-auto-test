# 示例环境配置（本地开发用，适配 Docker 里的 RuoYi v3.9.2 原版）
# 复制为 env.yaml 并修改；CI 用环境变量优先
env:
  base_url: "http://localhost:8080"
  web_url: "http://localhost:8081"
  tenant_id: ""

admin:
  username: "admin"
  password: "admin123"

db:
  host: "127.0.0.1"
  port: 13306
  user: "root"
  password: "password"
  database: "ry-vue"