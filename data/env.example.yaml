# 示例环境配置（本地开发用）
# 使用方式：复制为 env.yaml 并修改为实际值
# CI/CD 环境请使用环境变量，优先级高于 YAML

env:
  base_url: "http://localhost:48080"
  web_url: "http://localhost:80"
  tenant_id: "1"

admin:
  username: "admin"
  password: "admin123"

db:
  host: "localhost"
  port: 3306
  user: "root"
  password: ""   # ⚠ 改成你本地的密码
  database: "ruoyi-vue-pro"