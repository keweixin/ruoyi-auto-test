# 测试总结

## 1. 测试范围
本项目设计/实现 149 个测试实例，覆盖接口、UI、联动三类测试：
- 接口：登录、字典、部门、岗位、用户、角色、菜单
- UI：登录、字典、部门、岗位、用户、角色、菜单
- 联动：字典、用户、角色权限、UI操作后接口/DB校验

> 注意：简历建议写“设计/实现 149 个测试实例”，不要写“149 条全部通过”。实际通过数量以本地执行报告为准。

## 2. 执行环境
- 本地 Windows
- 后端：localhost:48080
- 前端：localhost:80
- MySQL + Redis
- pytest + requests + Playwright + Allure

## 3. 执行结果记录（真实全量执行 2026-06-30）

被测系统：Docker 中的 RuoYi v3.9.2 原版（后端 8080 + MySQL + Redis）。
注意：前端未启动，UI 用例自动跳过；联动改为纯接口联动。

| 类型 | 设计实例数 | 通过 | 失败 | 跳过 | 说明 |
|---|---:|---:|---:|---:|---|
| 接口 | 73 | 73 | 0 | 0 | 全部通过 |
| 接口联动 | 3 | 3 | 0 | 0 | 造数→验证→DB→清理闭环通过 |
| UI | 59 | 0 | 0 | 59 | 前端未启动，自动跳过 |
| 接口+UI联动 | 14 | 0 | 0 | 14 | 依赖前端，自动跳过 |

全量结果：**76 passed, 76 skipped, 0 failed**。
Allure 报告已生成：`reports/allure-report/index.html`。
真实结果文件：`reports/allure-results/`（985 个）。

## 4. 已知阻塞
- 前端未启动：原版 RuoYi-Vue 前端镜像无法拉取（Docker Hub 429），且无源码。UI 与接口+UI 联动用例自动跳过。
- 待前端就绪后，UI 与联动用例可恢复执行。


## 4. 已完成改进
- 共享 fixture 上移根 conftest，联动测试可访问
- logout 使用独立 token，避免污染 session token
- 日志与 Allure 脱敏
- 移除恒真断言
- 权限场景真实验证授权菜单/未授权菜单
- UI 删除增加 auto 前缀保护与唯一行检查
- Jenkins 报告清理策略修复

## 5. 风险
- UI 定位依赖前端版本，需根据本地页面微调
- JMeter 性能测试结果受本机硬件、数据库和Redis配置影响
- 全量通过数量必须以真实执行报告为准
