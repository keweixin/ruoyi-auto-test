# 测试总结

## 1. 测试范围
本项目设计/实现 149 个测试实例，覆盖接口、UI、联动三类测试：
- 接口：登录、字典、部门、岗位、用户、角色、菜单
- UI：登录、字典、部门、岗位、用户、角色、菜单
- 联动：字典、用户、角色权限、UI操作后接口/DB校验

> 注意：简历建议写“设计/实现 149 个测试实例”，不要写“149 条全部通过”。实际通过数量以本地执行报告为准。

## 2. 执行环境
- 本地 Windows
- 后端：localhost:48080
- 前端：localhost:80
- MySQL + Redis
- pytest + requests + Playwright + Allure

## 3. 执行结果（2026-07-01 最终报告）

被测系统：Docker RuoYi v3.9.2 原版（后端 8080 + 前端 8081 + MySQL + Redis）。

| 类型 | 通过 | 失败 | 说明 |
|---|---:|---:|---|
| API 接口 | 73 | 0 | 全部通过 |
| UI 测试 | 59 | 0 | 全部通过 |
| 接口联动 | 20 | 0 | 全部通过 |

全量：**152 passed, 0 failed, 0 skipped**。

Allure 报告：`reports/allure-report/index.html`（1642 个结果文件）。


## 4. 已完成改进
- 共享 fixture 上移根 conftest，联动测试可访问
- logout 使用独立 token，避免污染 session token
- 日志与 Allure 脱敏
- 移除恒真断言
- 权限场景真实验证授权菜单/未授权菜单
- UI 删除增加 auto 前缀保护与唯一行检查
- Jenkins 报告清理策略修复

## 5. 风险
- UI 定位依赖前端版本，需根据本地页面微调
- JMeter 性能测试结果受本机硬件、数据库和Redis配置影响
- 全量通过数量必须以真实执行报告为准
